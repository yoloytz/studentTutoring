<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tutoring.mapper.ClassesMapper">
    <insert id="insertClasses">
        insert into studenttutoring.classes(classname, classinfo, classmode, state, classstudentid, classtime, classplace)
        VALUES (#{className}, #{classInfo}, #{classMode}, #{state}, #{classStudentId}, #{classTime}, #{classPlace})
    </insert>

</mapper>